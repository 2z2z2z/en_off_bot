# Regression Testing — Phase 7

Дата: 2025-11-03  
Исполнитель: Codex (GPT-5)

## Автоматические проверки

- `npm run lint` — успешно, ошибок линтера нет.
- `npm test` — 11 тестовых наборов, 45 тестов, все прошли.
- `npm run test:perf` — успешно (временная SQLite-база).
  - Seed 500 пользователей: 1.59 сек (≈315 оп/с).
  - 1000 смешанных запросов: 0.87 сек (≈1153 оп/с).
  - Конкурентные обновления 50×20: 1.25 сек.

## Ручные проверки (не выполнены)

Требуется прогон сценариев из `docs/testing.md` (Telegram/VK, офлайн-очередь, админка, shutdown). Доступ к окружению отсутствует, поэтому тесты не запускались. Рекомендуемые шаги:

1. Telegram:
   - `/start` с новым пользователем, валидация клавиатуры.
   - Проверка буфера накопления, смены уровня, команды `/reset`.
2. VK:
   - Подтверждение работы клавиатур и inline-кнопок.
3. Офлайн-очередь:
   - Имитировать недоступность Encounter, проверить обработку очереди после восстановления.
4. Админка:
   - Добавление/удаление логинов через UI, проверка whitelist и модерации.

## Вывод

Автоматические проверки прошли успешно. Для завершения фазы 7 требуется выполнить ручное тестирование в боевом окружении и зафиксировать результаты.
